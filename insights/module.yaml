# The `insights` module — the blog / SEO content engine.
# List/article pattern: /insights/* auto-creates the list page (insights)
# and the inferred article page. Content lives in insights/posts/.

name: insights

pages:
  - insights:
      path: /insights/*
      content: posts

page_size: 10
