# The `white-papers` module — applied papers (gated / PDF later).
# List/article pattern: /white-papers/* auto-creates the list page
# (white-papers) and the inferred article page. Content in papers/.

name: white-papers

pages:
  - white-papers:
      path: /white-papers/*
      content: papers

page_size: 10
