# Products — LIST PAGE (Taj Mahal list/article module; 2026-06-29)

> ★ This is the **list page** of a Taj Mahal **list/article** module: `products: /products/*`
> → this list page + an inferred **article page** per product. Per the skill (Ch. 9), the list page
> renders an `articles` collection — **each card is generated from the article's frontmatter**
> (`title`, `productStatus` badge, `description`, `url`), ordered by `order`. So the cards in §2 are
> NOT hand-authored copy; they mirror what the template emits from the five article files
> (`_ia/products/*.md`). **To change a card, edit that product's frontmatter.** Doctrine: lead with
> the client's safety, not our brilliance; the in-production product is the proof; the rest carry
> honest status labels framed through the patient-maker lens.
> Status labels are calibrated to Governing Rule #1 (see each dossier): no version numbers, no
> readiness percentages, no "live" claims for things that aren't live, no fabricated scale.
> HOLD (not listed): Growthicious (planning), Taj Mahal Spaces (future).

---

## 1 · LIST-PAGE INTRO  *(static content above the auto-rendered list)*

# The engineering behind these products is what we bring to your system.

You need software that ships, runs in production, and that your own team can own and maintain
afterwards — not a science experiment, and not lock-in to one vendor's private tech. Everything
below is built on **mainstream technology you can hire for** — commonly Go, Python, React, and
PostgreSQL or MongoDB. We've put that same engineering into our **own** products — so before you
trust it on your system, you can see it running on ours.

> Honest labels on everything: what's shipped, what's in development, where each one stands
> today. Nothing here is dressed up as further along than it is.

**[ Get a free project estimate → ]**   ·   [ Talk to a human ]

`[Doctrine order: §1 leads with THEIR problem (ships, runs, you-own-it) and states the
no-lock-in / mainstream-tech reassurance up front, instead of opening with "what WE built." The
proof frame ("see it running on ours") sets up the in-production product (Processious) at the top
of the auto-rendered list. "Proven" claim removed — nothing on the page proves it, so we don't
assert it; the in-production product and the verifiable artifacts do the work. "We make
technology" stays the closer in §3.]`

`[PLACEHOLDER — needs Aamir: the strongest single line of CLIENT proof to seat in the hero
(e.g. the JP Morgan / 27-year track-record pedigree the Doctrine says to lead credibility with).
The Doctrine's #1 trust currency is client delivery proof; this page currently has none that's
verifiable. Add the verified pedigree line here once confirmed. Founder LinkedIn (verifiability
bridge for the JP Morgan / 27-year track record) = https://www.linkedin.com/in/aamironline —
supplied 2026-06.]`

---

## 2 · THE LIST  *(auto-rendered from each article's frontmatter, sorted by `order`)*

`[Each item = one card the list template emits per article: productStatus badge · title ·
description · "See the product →" → /products/<slug>/. Mirror of the five article files; do not
hand-edit here. Order = in-production first, then in-development / early-stage / internal.]`

**`[ In production ]`  Processious — our process-automation and application platform**
Our process-automation & application platform — in production, running a client's operations.
→ `/products/processious/`

**`[ In development ]`  Ordin — a workflow-automation engine**
A workflow-automation engine — YAML-defined workflows, pluggable activities; fair-code /
source-available.  → `/products/ordin/`

**`[ Early-stage ]`  Documentor.AI — describe the document you want; an AI drafts it from your own knowledge**
An AI document platform — describe the document you want; it drafts from your own knowledge so it
cites your facts.  → `/products/documentor/`

**`[ In development ]`  Enterprise Digital Asset Management**
Our digital-asset-management platform — offered today as a managed Enterprise DAM service.
→ `/products/tallery-gallery/`

**`[ Internal ]`  Booster — a dev-environment orchestrator**
Our dev-environment orchestrator — provisions a whole local stack from one config; we run our own
products on it.  → `/products/booster/`

`[Guardrails preserved from the article dossiers — do not weaken when the template renders these:
- Processious: ONE product called "Processious," "in production," NO version numbers, NO "% ready,"
  NO internal state; no invented metrics.
- Ordin: "In development" straight from the repo roadmap; license is "fair-code / source-available,"
  NOT "open source / MIT"; NO performance numbers; the pitch deck (ABC Corp / 99.999% / fake
  testimonials) stays quarantined.
- Documentor.AI: "Early-stage"; NEVER say available / live / beta-open; NO "100+ beta users"; NO
  prices; NO "pioneered/innovative" superlatives.
- Enterprise DAM (Tallery): the title is a CAPABILITY label — the "Tallery" brand is NOT surfaced
  on this list (Tallery V1 is private; the slug `tallery-gallery` is internal routing only). The
  managed DAM service is engageable today via a scoped pilot — NOT a self-serve product purchase;
  framework "In development." NEVER PUBLIC: the V1 failure, the COVID financial loss, "580 tests."
- Booster: "Internal" (dogfood proof); NO repo links, NO install commands, NO internal
  architecture; "open-source later," never "soon."]`

---

## 3 · WHY THE LIST LOOKS THIS WAY  *(patient-maker frame — re-risk defused)*

One of these products is in production; the rest are honestly labeled *in development*,
*early-stage*, or *internal*. That's deliberate, and it's the point: **we ship our own products
before we ship yours, and we mature them rather than abandon them.** You're not betting on a
roadmap promise — you're looking at work at every stage of a maker's pipeline, labeled exactly as
it stands. We only put a product on this page once it's real enough to label honestly; a couple
of things are still in planning, and you'll see them here when there's something true to say.

`[Directly answers Skeptic-Test BLOCKER #3 ("breadth reads as scattered — will mine be project
#18 they abandon?"). Reframes the volume of not-yet-shipped products through the patient-maker /
dogfood lens already used for Booster: maturity-not-abandonment, ship-our-own-first. Also holds
Growthicious (planning) and Taj Mahal Spaces (future) OFF the list while staying honest the
pipeline exists, without naming/teasing unannounced products. Remove if Aamir prefers to say
nothing about the pipeline.]`

---

## 4 · WHY WE BUILD OUR OWN  *(the depth — the closer, after the proof)*

### We don't just use technology — we make it. Which is the rigor your project inherits.

Most software firms assemble tools. We build our own — products, engines, even the foundations
underneath them. **You never have to adopt any of it: your project is built on mainstream
technology your own team can hire for and maintain.** Building at that depth is simply *why* our
engineering is as rigorous as it is — and, like everything we claim, you can check it yourself.

**[ Explore the lab → ]**   ·   [ Get a free project estimate → ]

`[The "we make technology" depth lands HERE as the closer, reframed the three ways the Doctrine
demands: BENEFIT ("the rigor your project inherits"), NO LOCK-IN ("mainstream tech, your own team
maintains"), VERIFIABLE ("check it yourself"). The unbacked comparative "runs deeper than most"
is CUT — the Doctrine explicitly warns that comparative superlatives from an unknown company
invite re-scrutiny; replaced with the plain causal "why our engineering is as rigorous as it is."
Closes with the low-risk CTA (free estimate) per Doctrine step 6.]`

---

`[note] LIST/ARTICLE WIRING (tajmahal-ssg Ch. 9): at site generation, module `products` declares
`products: /products/*`. The five `_ia/products/*.md` drafts are the article content, placed in the
`products/` content dir under clean slugs (`processious.md`, `ordin.md`, `documentor.md`,
`tallery-gallery.md`, `booster.md` → `/products/<slug>/`). Article frontmatter drives the cards:
`title · description · productStatus` (display badge — NOT the reserved `status`) `· order`
(curated, in-production first) `· image`. The list template loops `articles` sorted by `order`;
pagination via `page_size` if the set grows. Honest framing per product lives in each article's
own [note] + dossier. Growthicious (planning) and Taj Mahal Spaces (future) are held OFF the list.]`
